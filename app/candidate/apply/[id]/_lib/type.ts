export type TPhoneCountryCodes = {
  name: string;
  code: string;
};

export type TPhotoURL = {
  url: string;
  isSubmited: boolean;
};

export type TDialogMess = {
  title: string;
  body: string;
};
